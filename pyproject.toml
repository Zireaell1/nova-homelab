[project]
name = "nova-homelab"
version = "0.1.0"
description = "Ansible development environment for the nova-homelab repo"
requires-python = ">=3.14"
dependencies = [
    "ansible-core==2.21.4",
    "ansible-lint==26.8.0",
    "yamllint==1.38.0",
]
