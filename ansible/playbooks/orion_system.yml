---
- name: Orion system and foundation
  hosts: orion
  become: true
  roles:
    # =====================================================
    # Foundation and hardware
    # =====================================================
    - role: rpi_base
      tags: ["rpi_base"]

    - role: shell
      tags: ["shell", "foundation"]

    # =====================================================
    # Host monitoring
    # =====================================================
    - role: alloy
      tags: ["alloy", "host_monitoring"]
