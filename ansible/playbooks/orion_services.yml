---
- name: Orion rootless services
  hosts: orion
  become: false
  environment: "{{ podman_user_env }}"
  roles:
    # =====================================================
    # Runtime environment
    # =====================================================
    - role: dotfiles
      tags: ["dotfiles", "runtime"]

    # =====================================================
    # Services (Rootless)
    # =====================================================

    - role: eink_display
      tags: ["eink_display"]
