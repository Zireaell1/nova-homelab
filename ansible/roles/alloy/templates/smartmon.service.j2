[Unit]
Description=Generate SMART metrics for Prometheus (Daemon)
After=local-fs.target

[Service]
Type=simple
ExecStart=/usr/local/bin/smartmon_daemon.sh

Restart=always
RestartSec=10

StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
