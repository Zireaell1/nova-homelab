---
- name: Restart pihole
  ansible.builtin.systemd:
    daemon_reload: true
    name: pihole.service
    state: restarted
    enabled: true
    scope: user
  ignore_errors: "{{ ansible_check_mode }}"
