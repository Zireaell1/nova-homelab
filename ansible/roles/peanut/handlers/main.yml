---
- name: Restart peanut
  ansible.builtin.systemd:
    daemon_reload: true
    name: peanut.service
    state: restarted
    enabled: true
    scope: user
  ignore_errors: "{{ ansible_check_mode }}"
