---
- name: Restart frigate
  ansible.builtin.systemd:
    daemon_reload: true
    name: frigate.service
    state: restarted
    enabled: true
    scope: user
  ignore_errors: "{{ ansible_check_mode }}"
