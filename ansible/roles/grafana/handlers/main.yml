---
- name: Restart grafana
  ansible.builtin.systemd:
    daemon_reload: true
    name: grafana.service
    state: restarted
    enabled: true
    scope: user
  ignore_errors: "{{ ansible_check_mode }}"
