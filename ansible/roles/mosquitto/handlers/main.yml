---
- name: Restart mosquitto
  ansible.builtin.systemd:
    daemon_reload: true
    name: mosquitto.service
    state: restarted
    enabled: true
    scope: user
  ignore_errors: "{{ ansible_check_mode }}"
