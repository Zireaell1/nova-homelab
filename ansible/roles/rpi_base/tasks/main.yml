---
- name: Install system dependencies
  ansible.builtin.apt:
    name:
      - python3
      - python3-venv
      - python3-dev
      - python3-packaging
      - git
      - swig
      - liblgpio-dev
      - pipx
      - gpg
    state: present
    update_cache: true

- name: Verify if config directories exist
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "{{ item.mode }}"
  loop:
    - path: "/home/{{ ansible_user }}/.config/systemd/user"
      mode: "0755"

    - path: "/home/{{ ansible_user }}/.local/bin"
      mode: "0775"

- name: Enable lingering for current user
  ansible.builtin.command: "loginctl enable-linger {{ ansible_user }}"
  args:
    creates: "/var/lib/systemd/linger/{{ ansible_user }}"

- name: Provide a static DNS entry for Prometheus in /etc/hosts
  ansible.builtin.lineinfile:
    path: /etc/hosts
    regexp: '.*\b{{ services["prometheus"]["subdomain"] }}\.{{ vault_domain }}$'
    line: "{{ vault_primary_ip }} {{ services['prometheus']['subdomain'] }}.{{ vault_domain }}"
    state: present

- name: Enable SPI interface
  ansible.builtin.lineinfile:
    path: /boot/firmware/config.txt
    regexp: "^#?dtparam=spi="
    line: "dtparam=spi=on"
    state: present
  notify: Abort playbook if SPI was just enabled (manual reboot required)
