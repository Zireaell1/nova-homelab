[Unit]
Description=Immich Machine Learning (OpenVINO)
Documentation=https://docs.immich.app/

Wants=network-online.target
After=network-online.target

RequiresMountsFor={{ stacks_path }}/immich/model-cache

[Container]
# --- Image / Exec ---
Image={{ immich_machine_learning_image }}:{{ immich_machine_learning_version }}
ContainerName=immich-machine-learning
StopTimeout=20

# --- Identity / Namespaces ---
GroupAdd=keep-groups
UserNS=keep-id:uid=0,gid=0

# --- Security / Capabilities ---
DropCapability=all
NoNewPrivileges=true

# --- Resource Limits ---
Memory={{ immich_machine_learning_memory_limit }}

# --- Devices ---
AddDevice=/dev/dri/renderD128:/dev/dri/renderD128

# --- Storage ---
Volume={{ stacks_path }}/immich/model-cache:/cache:Z

# --- Networking ---
Network=immich-backend.network

# --- Configuration / Env ---
Timezone={{ timezone }}

EnvironmentFile={{ stacks_path }}/immich/immich-machine-learning.env

HealthCmd=["python3", "healthcheck.py"]
HealthInterval=60s
HealthTimeout=10s
HealthRetries=3
HealthStartPeriod=120s

[Service]
Restart=always
RestartSec=10
TimeoutStartSec=900
TimeoutStopSec=30

[Install]
WantedBy=default.target
